# Portfolio Version 2 Gate 2 Built State

Date: 2026-09-11

The version 2 portfolio is implemented as an additive, local-only
`/redesign` route on branch `redesign/v2`. The live homepage route and version
1 source remain intact.

The redesign presents Leo as a Systems Engineer focused on integration and
automation. It uses the approved headline, role line, project evidence,
capability matrix, My Approach sequence, conversation copy, and single primary
call action. AI and intelligent automation are visible as a distinct supporting
capability without becoming the primary professional identity.

The interface uses the approved lavender-led pastel system, self-hosted fonts,
responsive portrait derivatives, the three-node identity, light mode by
default, an optional persistent dark mode, a compact utility dock, and
purposeful motion with reduced-motion and reduced-transparency fallbacks.

The featured workflow uses native disclosure semantics and a replaying signal
animation that pauses when it should not run. All five projects retain the
approved evidence labels and locked prior-employer facts. No new employer,
client, metric, screenshot, repository, or implementation claim was added.

Automated local QA passes TypeScript, targeted lint, formatting, production
build, Chrome and Edge responsive coverage, keyboard operation, preference
modes, forced colors, no-JavaScript rendering, console review, privacy scans,
and social-image validation. Lighthouse accessibility and best-practices
confirmation scores are 100 on desktop and mobile. Leo reports that the human
Windows screen-reader check is complete.

The final mobile performance budget has not yet been scored against a
representative publication candidate. The current additive preview inherits
the version 1 root stylesheet and Google Fonts request by design. The approved
next phase is a local cutover candidate that points `/` to version 2, removes
the private preview route, replaces inherited version 1 head dependencies,
and reruns the final verification suite. No GitHub push, Vercel action, merge,
or production change has occurred.
